import React from "react";
import ReactDOM from "react-dom/client";
import DynamicAccessForm from "./DynamicAccessForm";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DynamicAccessForm />
  </React.StrictMode>
);
